# Smoke Test Plan

> Generated by `/plan-smoke-config` or created manually.
> This file describes how to validate the project locally before publishing a git-enabled task PR for human code review.

---

## Stack Summary

> *What stack is this project using, and what must be started before smoke checks run?*
> Example: Spring Boot API with PostgreSQL; start the database before running Maven tests and local app boot.

- **Primary stack:** [fill in]
- **Supporting services:** [fill in]
- **Start command:** [fill in]
- **Required environment variables:** [fill in]

Example:

- **Primary stack:** Java 21 / Spring Boot / Maven
- **Supporting services:** PostgreSQL via Docker Compose
- **Start command:** `./mvnw spring-boot:run`
- **Required environment variables:** `SPRING_PROFILES_ACTIVE=local`, `DATABASE_URL`

---

## Execution Order

1. Start every supporting service or fixture required by the stack.
2. Build or compile the project with the project's normal local command.
3. Start the application or worker process using the project-local configuration.
4. Run stack-specific smoke checks that prove the changed surface boots and responds correctly.
5. Collect evidence for human developer review.

---

## Smoke Checks

| # | Check | How to validate |
|---|-------|----------------|
| 1 | Supporting services are ready | [e.g. `docker compose up -d db` and `docker compose ps` shows healthy] |
| 2 | Application compiles and starts | [e.g. `./mvnw test` then `./mvnw spring-boot:run` reaches started state] |
| 3 | Connectivity or schema validation passes | [e.g. app logs show DB connection and migrations completed] |
| 4 | Changed surface responds correctly | [e.g. `curl http://localhost:8080/actuator/health` or a changed endpoint returns expected status] |
| 5 | No startup or migration regressions are visible | [e.g. logs contain no new stack traces, migration failures, or boot errors] |

## Smoke Evidence

For every smoke check that runs, record the execution evidence in the task file or a linked Markdown artifact.

| Check | Command | Parameters / profile / env | Environment | Config / scripts | Output log / report | Result |
|-------|---------|----------------------------|-------------|------------------|---------------------|--------|
| Supporting services | [exact command] | [env vars, profile, ports] | [local, Docker Compose, Testcontainers, CI] | [compose file, env file, helper script] | [log excerpt, report path, CI URL] | pass/fail/skipped + reason |
| App startup | [exact command] | [runtime profile, env vars] | [local app/service/worker] | [start script, config file] | [startup log excerpt or report] | pass/fail/skipped + reason |
| Changed surface | [exact command/check] | [URL, port, input fixture] | [local app + dependencies] | [.planning/SMOKE-TESTS.md, script, config] | [response/log excerpt or report] | pass/fail/skipped + reason |

---

## Database / ORM Consistency

Required for tasks that modify database structure, migrations, schema files, seed/bootstrap data, ORM models/entities, generated clients, repositories tied to ORM models, or persistence configuration.

| # | Check | How to validate |
|---|-------|----------------|
| 1 | Static DB/ORM consistency | [e.g. compare migration columns with entity fields, run ORM generate/check, then compile] |
| 2 | Local runtime environment | [e.g. start DB/services and app locally; ask the human if startup cannot be inferred] |
| 3 | Persistence smoke check | [e.g. create/read one non-destructive record through the changed repository or endpoint] |

---

## Notes For Automation

- If this file is missing, infer the smoke checks from the repository structure and build files before asking for human review.
- Prefer non-destructive commands and the smallest checks that still prove the real integration path works.
- Capture exact commands, parameters, environment, configuration/scripts, output logs or report links, and result for every smoke check that runs.
- If the project has migrations, schema generation, seed data, ORM models/entities, or generated DB clients, include static consistency checks and local runtime persistence smoke checks here.
- If the project has no backend or no external services, replace those rows with the closest reproducible local evidence.

---

## Human Review Gate

- Human developer review is required after smoke checks pass.
- If corrections are requested, rerun the impacted smoke checks before asking for approval again.
