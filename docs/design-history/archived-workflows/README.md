# Archived Workflows

These sub-workflows were removed from the installed `planning-template/` catalog because they were tied to an older SDLC-specific document model and were not invoked by current skills.

They are preserved here as design history only. Do not reference these files from active planning tasks unless they are redesigned and reintroduced into `planning-template/WORKFLOWS/`.

## Archived Files

| File | Reason archived |
|------|-----------------|
| `CHECK-DEVWORKFLOW-CONSISTENCY.md` | Assumes legacy `06-development/workflow/` template structure. |
| `CHECK-PHASE5-CHAIN.md` | Assumes legacy Phase 5 artifact chain: Roadmap -> Epics -> Use Cases -> Milestones -> Issue Mapping. |
| `CHECK-VERSIONING-ALIGNMENT.md` | Assumes legacy Phase 5/8/11 versioning links. |
